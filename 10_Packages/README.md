# Packages

This folder defines how ORBIT modules can be packaged for real use.

## Package Types

- Custom GPT package
- Studio workflow package
- Client delivery package
- Training package

## Package Rule

A package should include only the files required for its use case.

Do not bundle the whole repository by default.
